# Solver run output and experiment scratch (kept on disk, not tracked)
results_*/
ref_*procs/
ref_cp*/
compare_original/
__pycache__/

# Temp sub-simulation XMLs written by PartitionedFSI at run time
.partfsi_*_tmp.xml

# Unused per-step references (the test uses the single 1-proc result_*_001.vtu)
result_*_003.vtu

# Generated comparison plots / logs
diff_*.pdf
diff_*.png
compare_*.pdf
coupling_log.txt
coupling_performance.png
pressure_comparison.png
velocity_comparison.gif
monolithic_vs_partitioned*

# Experiment driver scripts / notes
tune_*.sh
run_*.sh
plot_diff.py
tolerance_study.md
solver_ramp_cp*.xml

# Analysis scripts (not used by the test)
compare_proc_diff.py
compare_fsi.py
