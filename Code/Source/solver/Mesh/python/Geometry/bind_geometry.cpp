// Geometry bindings (placeholder)
#include "bind_geometry.h"

namespace svmp_pybind {
void bind_geometry(pybind11::module_& m) {
  // TODO: add geometry helpers (e.g., compute normals, measures)
}
}

