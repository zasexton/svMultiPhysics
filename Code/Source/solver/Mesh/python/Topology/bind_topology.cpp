// Topology bindings (placeholder)
#include "bind_topology.h"

namespace svmp_pybind {
void bind_topology(pybind11::module_& m) {
  // TODO: add topology helpers (e.g., node_codim1, boundary_codim1)
}
}

